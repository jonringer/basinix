[workspace]
members = [
  "server",
  "shared",
  "worker",
  "evaluator",
]

[workspace.dependencies]
reqwest = { version = "0.11", features = [ "json", "blocking" ]}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"